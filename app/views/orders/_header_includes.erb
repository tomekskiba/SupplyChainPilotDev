<link rel="stylesheet" href="http://jqueryui.com/themes/base/jquery.ui.base.css">
<!--link rel="stylesheet" href="http://jqueryui.com/demos/demos.css"-->
<%#= stylesheet_link_tag 'calendar', :media => 'screen' %>
<%= stylesheet_link_tag 'jquery.ui.all.css', :media => 'screen' %>
<%= stylesheet_link_tag 'jquery.lightbox-0.5', :media => 'screen' %>

<%= javascript_include_tag "jquery-1.6.4" %>
<%= javascript_include_tag "jquery.ui.core" %>
<%= javascript_include_tag "jquery.ui.widget" %>
<%= javascript_include_tag "jquery.ui.datepicker" %>

<%= javascript_include_tag "jquery.lightbox-0.5" %>

<script>
    $(function() {
        //$("#order_supplies_counted_at").datepicker({dateFormat: 'yy-mm-dd'});

        $('a.lightBox').lightBox();
    });
</script>

<script>
    $(document).keydown(function(e) {
        var element = e.target.nodeName.toLowerCase();
        if ((element != 'input' && element != 'textarea') || $(e.target).attr("readonly")) {
            if (e.keyCode === 8) {
                return false;
            }
        }

        if (e.keyCode == 13) {
            return false;
            //$(':submit').attr('disabled', 'disabled');
        }
    });


    $(function () {
        $('input').keypress(function (e) {
            var code = null;
            code = (e.keyCode ? e.keyCode : e.which);
            return (code == 13) ? false : true;
        });
    });
</script>